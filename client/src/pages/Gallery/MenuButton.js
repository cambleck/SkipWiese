import React from "react";

const MenuButton = ({ icon, onClick }) => {
  return (
    <a className="menu-button ">
      <i className="material-icons menu-item">more_horiz</i>
    </a>
  );
};

export default MenuButton;
